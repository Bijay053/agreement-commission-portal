1) Sidebar Navigation: Agreement Status Menu
Requirement

Under Navigation → Agreements, add a status sub-menu (like WhatsApp/Email folders style).

Menu items (fixed order):

Draft

Active

Renewal in Progress

Expired

Terminated

Behavior

Clicking a status loads the Agreements list pre-filtered to that status.

The main “Agreements” item (parent) shows All Agreements (no status filter).

Optional (recommended): show count badges per status (e.g., Active: 12).

Routes (example)

/agreements → All

/agreements?status=draft

/agreements?status=active

/agreements?status=renewal_in_progress

/agreements?status=expired

/agreements?status=terminated

✅ Acceptance Criteria

Sidebar status click updates the list instantly (API call) and highlights the selected status.

2) Agreements List Page: Filters + Search Improvements
2.1 Search bar must support Provider Name

You want search by provider name.

Search should match:

Agreement title

Agreement code

Provider name

(optional) Provider code / short name

✅ Acceptance

Typing “Deakin” returns agreements whose provider is “Deakin University”.

2.2 Add Filter Column: Provider Name

Add a dropdown / autocomplete filter:

Label: Provider

Type: searchable dropdown (select provider)

Supports clear/reset

✅ Acceptance

Selecting a provider shows only agreements for that provider.

2.3 Add Filter Column: Country (Two options)

You said “search provider name and by country”. You also previously requested provider country too, so define clearly:

Must include at least:

Territory Country filter (agreement territory)

Recommended (best): include both:

Territory Country (agreement territory)

Provider Country (provider’s country)

If you want only one, use Territory Country.

✅ Acceptance

Selecting Bangladesh shows agreements where territory includes Bangladesh (or global handling below).

3) Status-based List Logic (Data rules)
Status values (backend canonical)

draft

active

renewal_in_progress

expired

terminated

Global territory handling (important)

If an agreement is Global territory:

It should appear in country filter only if:

user selects “All Countries”, or

you add special country option “Global”

Recommended UI option in country filter:

All Countries

Global

then list of countries

✅ Acceptance

Country filter doesn’t incorrectly hide global agreements unless user filters to a specific country.

4) Backend API Requirements
Endpoint

GET /agreements

Query Params

status (optional): draft|active|renewal_in_progress|expired|terminated

q (optional): search text (agreement title/code/provider)

provider_id (optional)

territory_country_id (optional) OR country_id depending on your model

pagination: page, page_size

sorting: sort=expiry_date (optional)

✅ Acceptance

Same endpoint supports sidebar + filters + search together.

5) UI Layout Update (Recommended)

Top filter row should contain:

Search (q)

Status dropdown (still ok, but sidebar will be primary)

Provider filter

Country filter

✅ Acceptance

User can filter by Status + Provider + Country at the same time.